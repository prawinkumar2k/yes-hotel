# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/

# Playwright output
test-results/
playwright-report/
blob-report/
playwright/.cache/

# .env holds real secrets for this project (MongoDB URI, JWT secrets,
# Razorpay/Cloudinary/SMTP credentials) — it must never be committed.
# .env.example (tracked) documents the required keys with placeholder values.
.env
.env.local
.env.*.local

# Local AI-coding-tool scaffolding/state — machine-local, not part of the
# application, and in .jetro's case contains an actual credentials.json.
# None of this belongs in the project repository.
.jetro/
.cursor/
.agents/
.builder/
.windsurfrules
.mcp.json
AGENT.md
CLAUDE.md
